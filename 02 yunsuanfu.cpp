#include <iostream>
using namespace std;

int main(){
	
	// 1.算数运算符 用于四则运算
	// +() -() + - * / % ++() ()++ ()-- --()
	
	
	// 2.赋值运算符 用于将表达式的值赋给变量
	// = += -+ *= /= %=
	// a += b --> a = a + b, 其他的类比
	
	// 3.比较运算符 用于表达式的比较，返回一个真值或假值
	// == != > < >= <=
	
	// 4.逻辑运算符 根据表达式的值返回真值或假值
	// ! && ||
	
	
	
	
	
	
	system("pause");
	return 0;
}
